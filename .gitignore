node_modules
.next
out
dist
coverage
tsconfig.tsbuildinfo

# env
.env
.env.local
.env.*.local

# db
prisma/migrations/migration_lock.toml.bak

# local backups (full db + uploads archives)
backups/

# uploads (user data)
uploads/

# ghost prompt bakeoff outputs + local fixture photos
tmp/
fixtures/ghost/*
!fixtures/ghost/README.md

# on-device embedding model + ONNX runtime, staged into /public by
# `pnpm embedding:fetch` (runs as part of `build`). ~73 MB of binary that would
# otherwise sit in git history forever and be re-cloned by everyone.
public/models/
public/ort/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os
.DS_Store

# editors
.vscode/*
!.vscode/extensions.json
.idea

# typescript
*.tsbuildinfo

# staged face models (pnpm face:fetch)
.models/
